VITE_RAZORPAY_KEY_ID=rzp_test_your_key_id_here
# Backend API base URL. Leave unset for local dev (defaults to localhost:5000).
# Set this to your deployed backend's URL (e.g. Render) when deploying the
# frontend to Vercel or anywhere else.
VITE_API_BASE_URL=http://localhost:5000
